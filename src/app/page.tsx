import MapSection from '@/components/MapSection';
import React from 'react';

const page = () => {
  return (
    <main className="w-full h-full">
      <MapSection />
    </main>
  );
};

export default page;
