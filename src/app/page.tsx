import MapSection from '@/components/MapSection';
import React from 'react';

const page = () => {
  return (
    <main className="w-full h-screen">
      <MapSection />
    </main>
  );
};

export default page;
